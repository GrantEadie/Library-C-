@{
  Layout = "_Layout";
}

@using Library.Models;
@model List<Library.Models.Book>;

<h1>Books</h1><hr>
  <div class="row">
    <div class="col-sm">
      @if (@Model.Count == 0)
{
  <p>No Books have been added yet!</p>
} 

@foreach (Book book in Model)
{
  <p class="list-details">@Html.ActionLink($"{book.BookName}", "Details", new { id = book.BookId })</p>
}
<br><br>

<p>@Html.ActionLink("Add a new book", "Create", null, null, new { @class="btn btn-outline-dark btn-block"} )</p>

<p>@Html.ActionLink("Home", "Index", "Home", null, new { @class="btn btn-outline-dark btn-block"} )</p>
    </div>
    <div class="col-sm">
    </div>
    <div class="col-sm">
    </div>
  </div>